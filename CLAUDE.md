# Codeboard agent notes

Follow the development and reload workflow in `AGENTS.md`. In particular, never reload Codeboard while edits are incomplete; run tests first and invoke `./scripts/codeboardctl reload` only at a coherent verified point.
